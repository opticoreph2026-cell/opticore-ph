import PricingClient from './PricingClient';

export const metadata = {
  title: 'Pricing — OptiCore PH',
  description: 'Simple, honest pricing for AI utility optimization. Start free, upgrade when you need more properties or deeper analysis.',
};

export default function PricingPage() {
  return <PricingClient />;
}

